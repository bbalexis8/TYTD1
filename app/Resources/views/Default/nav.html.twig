{% block nav %}

    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Parcourir le menu</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#"><img id="logonav" alt="Brand" src="{{ asset('image/Logo x48.png') }}"></a>
            </div>

            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="{{ path('accueilsite') }}">Accueil</a></li>
                    <li><a href="{{ path('articleslist') }}" id ="blog">Blog</a></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Assistant <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('eventcategorieslist') }}">Liste des évènements</a></li>
                            <li role="separator" class="divider"></li>
                            <li>Catégories :</li>
                            {#{% for cat1 in categorie %}#}
                                {#<li><a href="{{ path('categorie_show', { 'id': categorie.id }) }}">{{ categorie.nomCa }}</a></li>#}
                                {#<br>#}
                            {#{% endfor %}#}
                        </ul>
                    </li>
                    <li><a href="{{ path('temoignageslist') }}">Témoignages</a></li>
                    <li><a href="{{ path('utilisateur_index') }}">Partenaires</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#">Contactez-nous <span class="glyphicon glyphicon glyphicon-pencil" aria-hidden="true"></span></a></li>
                    <li><a href="{{ path('homeAdmin') }}">Espace admin <span class="glyphicon glyphicon glyphicon-lock" aria-hidden="true"></span></a></li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>

{% endblock %}

