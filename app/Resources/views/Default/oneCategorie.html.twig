{% extends ':Default:basic.html.twig' %}

{% block body %}
    <div id="pagecat" class="container col-xs-12 text-center">
        <div class="col-xs-6 col-xs-offset-3">

            <div id="divtem" class="row text-justify">
                <h2><span class="glyphicon   glyphicon glyphicon-pencil"></span> «{{ categorie.nomCa }}»</h2>
                <img src="{{ categorie.imageCa }}"><br><br>
                <p>Liste des évenements liés à cette catégorie :</p>
                {% for evenement in evenements %}

                    <h2>
                        <a href="{{ path('evenement_show', {'id':categorie.id}) }}" class="thumbnail">
                            {{ evenement.nom }}</a></h2>
                    <p>{{ evenement.dateE|date('d/m/Y') }}</p>
                    <p>par {{ evenement.utilisateur.username }}</p><hr>

                {% endfor %}
                <br>
                <a id="btnlistemcpt" href="{{ path('eventcategorieslist') }}"
                   class="btn btn-default col-xs-4 col-xs-offset-6">

                    <span class="glyphicon  glyphicon glyphicon-chevron-right "></span> Retour à la liste des catégories</a>
            </div>
            <br><br>
        </div>
    </div>
{% endblock %}

