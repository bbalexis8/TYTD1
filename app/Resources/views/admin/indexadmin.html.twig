{% extends 'admin/adminbase.html.twig' %}

{% block body %}
    <div class="bobody">
        <h2>Dernières publications ou modifications</h2>

        <div>
            {% for article in articles %}
            <p>
                <h3> <a href="{{ path('article_show', { 'id': article.id }) }}">N°{{ article.id }}</a>
                {{ article.titre }}</h3>
                <br>{{ article.description }}
                {#<br>{{ article.utilisateur.username }}#}
            </p>
                {% endfor %}
        </div>


    </div>
{% endblock %}
