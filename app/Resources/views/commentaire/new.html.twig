{% extends 'admin/adminbase.html.twig' %}

{% block body %}
    <div class="bobody">
        <h2>Rédaction d'un Commentaire</h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}<br>
        <input type="submit" value="Valider ce commentaire"/>
        {{ form_end(form) }}
        <hr>

        <ul>
            <li>
                <a href="{{ path('commentaire_index') }}">Voir liste complète des commentaires</a>
            </li>
        </ul>
        <hr>
    </div>
{% endblock %}
