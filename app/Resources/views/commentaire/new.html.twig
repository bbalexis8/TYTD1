{% extends 'admin/adminbase.html.twig' %}

{% block body %}
    <div class="bobody">
        <h2>Rédaction d'un Commentaire</h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer"/>
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('commentaire_index') }}">Voir liste</a>
            </li>
        </ul>
    </div>
{% endblock %}
