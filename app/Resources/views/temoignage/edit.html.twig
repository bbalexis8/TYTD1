{% extends 'admin/adminbase.html.twig' %}

{% block body %}
    <h2>Modification d'un témoignage</h2>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}<br>
    <input type="submit" value="Valider les modifications"/><br>
    {{ form_end(edit_form) }}


    <div class="btn-group  divine btn-group-justified" role="group" aria-label="...">
        <div class="btn-group ">
            <a href="{{ path('temoignage_index') }}"><br>
                Retour à la liste </a>
            <br>
        </div>

    </div>
    <div>

        {{ form_start(delete_form) }}
        <input type="submit" value="Supprimer">
        {{ form_end(delete_form) }}
        <hr>
    </div>

{% endblock %}
